name: get_base
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # 状态管理
  get: ^4.6.5

  # 网络请求
  dio: ^5.0.0
  pretty_dio_logger: ^1.3.1 #打印输出

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
